import { PlaygroundDirective } from './playground.directive';

describe('PlaygroundDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaygroundDirective();
    expect(directive).toBeTruthy();
  });
});
