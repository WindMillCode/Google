import { SignPadDirective } from './sign-pad.directive';

describe('SignPadDirective', () => {
  it('should create an instance', () => {
    const directive = new SignPadDirective();
    expect(directive).toBeTruthy();
  });
});
