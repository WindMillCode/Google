import { PrintFilesDirective } from './print-files.directive';

describe('PrintFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintFilesDirective();
    expect(directive).toBeTruthy();
  });
});
