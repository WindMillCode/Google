import { ToggleButtonDirective } from './toggle-button.directive';

describe('ToggleButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleButtonDirective();
    expect(directive).toBeTruthy();
  });
});
