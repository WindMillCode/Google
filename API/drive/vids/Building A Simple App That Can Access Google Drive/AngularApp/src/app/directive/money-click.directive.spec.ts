import { MoneyClickDirective } from './money-click.directive';

describe('MoneyClickDirective', () => {
  it('should create an instance', () => {
    const directive = new MoneyClickDirective();
    expect(directive).toBeTruthy();
  });
});
