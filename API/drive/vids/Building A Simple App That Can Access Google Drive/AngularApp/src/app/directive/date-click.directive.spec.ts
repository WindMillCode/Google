import { DateClickDirective } from './date-click.directive';

describe('DateClickDirective', () => {
  it('should create an instance', () => {
    const directive = new DateClickDirective();
    expect(directive).toBeTruthy();
  });
});
