import { FileHandlerDirective } from './file-handler.directive';

describe('FileHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new FileHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
