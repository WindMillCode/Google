import { InputHandleDirective } from './input-handle.directive';

describe('InputHandleDirective', () => {
  it('should create an instance', () => {
    const directive = new InputHandleDirective();
    expect(directive).toBeTruthy();
  });
});
