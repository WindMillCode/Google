import { FieldsDirective } from './fields.directive';

describe('FieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldsDirective();
    expect(directive).toBeTruthy();
  });
});
